name: pulse_transform
version: '1.0'
config-version: 2
profile: pulse_transform_profile

model-paths: [models]
analysis-paths: [analysis]
macro-paths: [macros]

target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  pulse_transform:
    +materialized: table
    +schema: public
